## Packages
framer-motion | Smooth animations for page transitions and interactions
date-fns | Formatting dates nicely (e.g., "2 days ago")
react-hook-form | Robust form handling
@hookform/resolvers | Zod validation for forms

## Notes
Integration with existing Shadcn UI components in @/components/ui
Authentication uses simple username-based flow (api.auth.login)
Public pages accessible via /p/:token do not require auth
