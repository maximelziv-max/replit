Ниже — Super Prompt для Replit AI, который учитывает твой репозиторий (Vite+React+TS / Express / Drizzle) и сразу описывает ВСЕ задачи:
✅ Card/Table view оферов + expand (Sheet) + статусы + удаление + bulk actions
✅ AI-помощник (лёгкая модель “GPT-4 fast / mini”) для ТЗ и оферов
✅ Безопасность: ключ только на server, лимиты, валидации

Скопируй и вставь в Replit AI целиком.

Ты — AI-разработчик в проекте Replit. В репозитории: client/ (React+Vite+TS), server/ (Express), shared/schema.ts (Drizzle схема). Нужно реализовать обновление vNext+++.

ОБЩАЯ ЦЕЛЬ:
1) Улучшить экран оферов у заказчика: добавить режим "Таблица (Excel)" + expand, статусы, удаление, массовые действия.
2) Добавить базовый AI-помощник (лёгкая модель, например gpt-4o-mini / fast) для улучшения текста ТЗ и текста оферов. AI должен только помогать писать, не выбирать кандидата.

========================
ЧАСТЬ A) OFFERS UI + BULK
========================

A1) База данных / Drizzle
- Файл: shared/schema.ts
- В таблицу offers добавить поле:
  status: text("status").notNull().default("new")
- status принимает значения: "new" | "shortlist" | "rejected"

Сделай миграцию Drizzle если проект использует миграции; если миграций нет, обеспечь чтобы dev DB не падала и схема применялась.

A2) Storage слой
- Файл: server/storage.ts
Добавить в интерфейс IStorage:
- updateOfferStatus(offerId: number, status: "new"|"shortlist"|"rejected"): Promise<Offer>
- deleteOffer(offerId: number): Promise<void>
- updateOffersStatus(offerIds: number[], status: "new"|"shortlist"|"rejected"): Promise<Offer[]>
- deleteOffers(offerIds: number[]): Promise<{ deleted: number }>

Реализовать в DatabaseStorage:
- updateOfferStatus: UPDATE offers SET status=? WHERE id=? RETURNING *
- deleteOffer: DELETE FROM offers WHERE id=?
- updateOffersStatus: UPDATE offers SET status=? WHERE id IN (...) RETURNING *
- deleteOffers: DELETE FROM offers WHERE id IN (...) ; вернуть deleted count

A3) Routes/API
- Файл: server/routes.ts
Добавить endpoints:

1) PATCH /api/offers/:id/status
   body: { status: "new"|"shortlist"|"rejected" }
   validate zod enum, вернуть обновлённый offer

2) DELETE /api/offers/:id
   вернуть { ok: true }

3) PATCH /api/offers/bulk/status
   body: { offerIds: number[], status: "new"|"shortlist"|"rejected" }
   validate: offerIds.length>=1, status enum
   вернуть массив updated offers

4) DELETE /api/offers/bulk
   body: { offerIds: number[] }
   validate: offerIds.length>=1
   вернуть { deleted: N }

A4) Frontend: режим Card/Table
- Файл: client/src/pages/project-details.tsx
В блоке "Полученные оферы" (TabsContent value="offers"):
- Добавить toggle view: "Карточки" | "Таблица"
- Default: если offers_count > 10 => table else cards
- Сохранять выбор в localStorage key: offers_view_mode_project_${project.id}

A5) Компонент таблицы оферов (Excel view)
Создать: client/src/components/OffersTable.tsx
Использовать shadcn Table (client/src/components/ui/table.tsx).
Требования:
- Строка = 1 offer
- Колонки:
  - Checkbox selection (bulk)
  - Исполнитель (freelancerName)
  - Контакт (contact, показывать как текст + кнопка copy или tg/mail link если возможно)
  - Цена (price)
  - Срок (deadline)
  - Подход (approach): truncate 1-2 строки + кнопка "Развернуть"
  - Гарантии (guarantees): truncate + "Развернуть"
  - Риски (risks): truncate + "Развернуть"
  - Статус (badge new/shortlist/rejected)
  - Действия: ⭐ shortlist, ❌ rejected, 🗑️ delete, "Связаться"
- Добавить сортировки:
  - по дате (default)
  - по цене
  - по сроку
  Реализация MVP: извлекать число из price regexом, deadline сортировать строкой.
- Добавить фильтр по статусу (all/new/shortlist/rejected).
- Checkbox "Select all" в заголовке выбирает только видимые оферы (с учетом фильтра).

A6) Bulk Action Bar
В OffersTable или в project-details добавить панель массовых действий, которая появляется, если selectedOfferIds.size>0:
- текст: "Выбрано: X"
- кнопки: ⭐ В шорт-лист, ❌ Отклонить, 🗑️ Удалить, "Снять выделение"
Bulk calls:
- PATCH /api/offers/bulk/status
- DELETE /api/offers/bulk (с подтверждением)

A7) Expand через Sheet (лучший UX)
Создать: client/src/components/OfferDetailsSheet.tsx
Использовать shadcn Sheet (client/src/components/ui/sheet.tsx).
Открывается:
- по клику "Развернуть" в ячейке
- или по клику по строке
Показывает полный offer:
- имя + контакт + портфолио + опыт + навыки
- полный подход/гарантии/риски
- цена/срок
- действия: shortlist/rejected/delete/связаться

A8) Удаление офера (single + bulk)
Использовать shadcn AlertDialog (client/src/components/ui/alert-dialog.tsx).
- Single delete: иконка 🗑️ в карточках, таблице и Sheet. Подтверждение:
  "Удалить офер? Офер будет удалён без возможности восстановления."
  -> DELETE /api/offers/:id
- Bulk delete: подтверждение:
  "Удалить выбранные оферы? Будет удалено X оферов. Нельзя отменить."
  -> DELETE /api/offers/bulk

A9) Карточный режим
В существующих карточках оферов (project-details.tsx):
- добавить badge статуса
- добавить действия shortlist/reject/delete (с подтверждением delete)
Карточки должны синхронизироваться с таблицей (после изменения статуса/удаления обновлять UI).

=============================
ЧАСТЬ B) AI-помощник (GPT fast)
=============================

B1) ENV
- Добавить в server env поддержку OPENAI_API_KEY.
Не хранить ключ на клиенте.

B2) Новый файл server/ai.ts
Сделать функцию вызова OpenAI API (fetch) с моделью лёгкой (gpt-4o-mini или аналог fast).
Функции должны возвращать строго JSON.

B3) Rate limit и лимиты текста
- Ограничить размер входного текста (например 5000 символов).
- Простейший rate limit per session/user для AI endpoints (например 20 запросов/час).
Можно сделать в памяти (Map) для MVP.

B4) Endpoints AI (server/routes.ts)
Добавить:
1) POST /api/ai/project/improve
   body: { title, description, result, deadline, budget, template }
   return: { suggested_description, suggested_result, improvements[], missing_info[] }

2) POST /api/ai/project/review
   body: { template, description, result, deadline, budget }
   return: { improvements[], missing_info[] }

3) POST /api/ai/offer/improve
   body: { project:{...}, offer:{ approach, deadline, price, guarantees, risks }, template }
   return: { suggested_offer:{ approach, guarantees, risks }, improvements[] }

4) POST /api/ai/offer/review
   body: { project:{...}, offer:{...}, template }
   return: { improvements[], missing_info[] }

B5) Prompts (важно)
- AI не должен выдумывать данные.
- Если данных не хватает — перечислять в missing_info.
- Ответы короткие, структурные, без воды.
- AI не выбирает кандидата, не оценивает "брать/не брать".

B6) Frontend: AI кнопки в создании проекта (ТЗ)
На странице создания проекта (где форма ТЗ):
- кнопка ✨ "Сделать описание структурнее" -> /api/ai/project/improve
  показать preview результата и кнопку "Применить" (не применять автоматически).
- кнопка 💡 "Совет по ТЗ" -> /api/ai/project/review
  показать список improvements и missing_info

B7) Frontend: AI кнопки в форме офера (исполнитель)
На странице формы офера:
- ✨ "Сделать офер структурнее" -> /api/ai/offer/improve
  preview и apply (только approach/guarantees/risks, не менять цену/срок).
- 💡 "Совет по оферу" -> /api/ai/offer/review
  показать improvements/missing_info

B8) UI требования
- Показывать loading состояние.
- Обрабатывать ошибки (нет ключа, лимит, сеть).
- Preview-before-apply обязателен.

========================
ЧАСТЬ C) Acceptance Criteria
========================

Фича готова если:
1) Есть toggle Card/Table на оферах.
2) Таблица включает truncate + expand через Sheet.
3) Есть статусы offer: new/shortlist/rejected, с фильтром и сортировками.
4) Есть single delete и bulk delete с подтверждением.
5) Есть bulk shortlist/reject по выбранным оферам.
6) Все изменения отражаются и в таблице, и в карточках.
7) AI endpoints работают с server-side ключом, с лимитами.
8) На ТЗ форме и на форме офера есть 2 AI кнопки (improve/review) и preview/apply.

Выполняй изменения аккуратно: не ломай существующую структуру страниц. Где возможно — создавай новые компоненты в client/src/components/ и переиспользуй их. После внесения изменений убедись, что TypeScript сборка проходит, и что API маршруты не конфликтуют с существующими.

Если хочешь, я могу дополнительно:
	•	подсказать куда именно в твоём проекте вставить AI-кнопки (названия файлов/компонентов в zip),
	•	или сделать супер-промпт ещё жёстче: “открой файл X, вставь блок после строки Y”.